import React from "react"
import { Select, Input } from "antd"
import md from "@/config/markdownConfig"
import style from "./index.module.scss"
const Publish: React.FC = () => {
  return <div className={style.index}></div>
}